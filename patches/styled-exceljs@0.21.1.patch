diff --git a/xlsx.js b/xlsx.js
index f215e16d55c6bf9cf790e823ad6322488ebc9bfe..fe4fa99d68a8ecc8b7774d32556e0dd2c6668820 100644
--- a/xlsx.js
+++ b/xlsx.js
@@ -16760 +16760,2 @@ function parse_ws_xml_cols(columns, cols) {
-		if(!seencol && coll.width) { seencol = true; find_mdw_colw(coll.width); }
+		/* Column-width inference must not inherit the previous workbook's MDW. */
+		if(!seencol && coll.width) { seencol = true; MDW = DEF_MDW; find_mdw_colw(coll.width); }
@@ -17930 +17931 @@ function parse_ws_bin(data, _opts, idx, rels, wb, themes, styles) {
-					if(!seencol) { seencol = true; find_mdw_colw(val.w/256); }
+					if(!seencol) { seencol = true; MDW = DEF_MDW; find_mdw_colw(val.w/256); }
@@ -21470 +21471 @@ wb.opts.Date1904 = Workbook.WBProps.date1904 = val; break;
-						if(!seencol) { seencol = true; find_mdw_colw(val.w/256); }
+						if(!seencol) { seencol = true; MDW = DEF_MDW; find_mdw_colw(val.w/256); }
diff --git a/xlsx.mjs b/xlsx.mjs
index 114fa2cd3caa8ca12ca3f1ae49b70c794549b121..c917519dc7ee80c3ca7976b4666135bb705c743e 100644
--- a/xlsx.mjs
+++ b/xlsx.mjs
@@ -16849 +16849,2 @@ function parse_ws_xml_cols(columns, cols) {
-		if(!seencol && coll.width) { seencol = true; find_mdw_colw(coll.width); }
+		/* Column-width inference must not inherit the previous workbook's MDW. */
+		if(!seencol && coll.width) { seencol = true; MDW = DEF_MDW; find_mdw_colw(coll.width); }
@@ -18020 +18021 @@ function parse_ws_bin(data, _opts, idx, rels, wb/*:WBWBProps*/, themes, styles)/*
-					if(!seencol) { seencol = true; find_mdw_colw(val.w/256); }
+					if(!seencol) { seencol = true; MDW = DEF_MDW; find_mdw_colw(val.w/256); }
@@ -21573 +21574 @@ function parse_workbook(blob, options/*:ParseOpts*/)/*:Workbook*/ {
-						if(!seencol) { seencol = true; find_mdw_colw(val.w/256); }
+						if(!seencol) { seencol = true; MDW = DEF_MDW; find_mdw_colw(val.w/256); }
@@ -26216,7 +26217,10 @@ var numbers_lut_new = function() {
 function numbers_format_cell(cell, t, flags, ofmt, nfmt) {
   var _a, _b, _c, _d;
   var ctype = t & 255, ver = t >> 8;
-  var fmt = ver >= 5 ? nfmt : ofmt;
+  // Older Numbers 3 documents can reference a cell format while omitting the
+  // corresponding legacy/new format table. Treat that as the default format
+  // instead of crashing while probing duration fields on `undefined`.
+  var fmt = (ver >= 5 ? nfmt : ofmt) || [];
   dur:
     if (flags & (ver > 4 ? 8 : 4) && cell.t == "n" && ctype == 7) {
       var dstyle = ((_a = fmt[7]) == null ? void 0 : _a[0]) ? varint_to_i32(fmt[7][0].data) : -1;
