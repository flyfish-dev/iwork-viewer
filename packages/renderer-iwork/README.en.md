# @file-viewer/renderer-iwork

File Viewer renderer adapter for `iwork-viewer`. It connects format registration, Worker paths, zoom, export, and thumbnail contracts without owning the parser or scene renderer.
